# Server Configuration
PORT=3001
NODE_ENV=production

# PostgreSQL Configuration (Coolify / Supabase self-hosted)
# NOT: POSTGRES_HOST genelde aynı docker network içindeki servis adıdır (çoğunlukla "postgres" veya "db")
POSTGRES_HOST=postgres
POSTGRES_PORT=5432
POSTGRES_DB=postgres

# ÖNERİLEN: Supabase reserved role'ları yerine ayrı bir kullanıcı kullanın (örn: proxy_user)
POSTGRES_USER=proxy_user
POSTGRES_PASSWORD=CHANGE_ME

# WATI API ayarları Postgres içinde `settings` tablosunda saklanır:
# - wati_api_key
# - wati_api_url

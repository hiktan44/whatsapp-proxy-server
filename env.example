# Server Configuration
PORT=3001
NODE_ENV=production

# Supabase (server-side)
# ÖNEMLİ: Service Role Key sadece proxy server'da olmalı (frontend'e koymayın)
SUPABASE_URL=https://YOUR-SUPABASE-KONG-DOMAIN:8000
SUPABASE_SERVICE_ROLE_KEY=YOUR_SERVICE_ROLE_KEY

# Opsiyonel: CORS origin whitelist (virgülle)
# Örn: CORS_ORIGINS=https://your-app.netlify.app,http://localhost:8080
CORS_ORIGINS=*
